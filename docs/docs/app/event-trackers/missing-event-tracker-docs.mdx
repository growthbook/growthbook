import Link from '@docusaurus/Link';

export default function MissingEventTrackerDocs({ eventTracker, slug }) {
    return (
        <>
            <p>
                Unfortunately, even though Growthbook fully works with { eventTracker }, a How-To guide for {eventTracker} hasn't been written yet.  We do have a thriving developer community on <Link to={`https://slack.growthbook.io?ref=docs-eventTracker-${slug}`}>Slack</Link> that you can reach out to in order to get your implementation working. 
            </p>
            <p>
                We are a small open source company, so when you do, please consider contributing back to the community by <Link to="https://github.com/growthbook/growthbook/blob/main/CONTRIBUTING.md#working-on-docs">creating this page</Link>. 
            </p>
        </>
    );
}
