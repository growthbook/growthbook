If you use [GrowthBook Cloud](https://app.growthbook.io) and the destination is behind a firewall, allow inbound traffic from `52.70.79.40/32`. That is the only address GrowthBook Cloud connects out from — see [IP addresses](/ip-addresses).
