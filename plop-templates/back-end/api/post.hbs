import { post{{pascalCase object}}Validator } from "shared/validators";
import { createApiRequestHandler } from "back-end/src/util/handler";

export const post{{pascalCase object}} = createApiRequestHandler(post{{pascalCase object}}Validator)(
  async (req) => {
    const {{camelCase object}} = await req.context.models.{{camelCase object}}s.create(req.body);

    return {
      {{camelCase object}}: req.context.models.{{camelCase object}}s.toApiInterface({{camelCase object}}),
    };
  }
);
