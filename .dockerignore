# Build and test artifacts
.git
.github
# CI vendors the growthbook/skills checkout here and bundles before the image
# build, so the source tree itself never needs to enter the build context.
skills-src
.gitignore
Dockerfile
docker-compose.yml
.dockerignore
.eslint*
.prettier*
.husky
*.md
!packages/back-end/generated/agent-skills/**/*.md
!packages/back-end/src/agent/skills-local/**/*.md
**/node_modules
**/coverage
**/.next
**/dist
**/*.log
**/dummy

# Docs
./docs

# Secrets / private data
**/*.env.local
**/config.yml
packages/back-end/uploads

# Python notebooks and caches
packages/stats/**/.pytest_cache
packages/stats/**/__pycache__

# Test directories
packages/back-end/test
packages/front-end/test

# Unneeded dev requirements that get caught in security scans
packages/stats/dev-requirements.txt