import React, { FC } from "react";

type {{pascalCase component}}Props  = Record<string, never>; // TODO: Replace with props object

export const {{pascalCase component}}: FC<{{pascalCase component}}Props> = (_props) => {
  return (
    <div>
      <h1>{{pascalCase component}}</h1>
    </div>
  )
}

export const {{pascalCase component}}Container = () => {
  return (
    <{{pascalCase component}} />
  )
}
