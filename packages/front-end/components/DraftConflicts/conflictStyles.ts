export const CONFLICT_VALUE_STYLE = {
  background: "var(--color-surface)",
  borderRadius: "var(--radius-1)",
  padding: "1px 6px",
  whiteSpace: "pre-wrap" as const,
  overflowWrap: "anywhere" as const,
  minWidth: 0,
};
