{
  "name": "stats-ts",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "build:clean": "rimraf dist tsconfig.tsbuildinfo",
    "build:typescript": "tsc",
    "dev": "swc src -w --out-dir dist",
    "build": "pnpm build:clean && pnpm build:typescript",
    "test": "vitest run",
    "clean": "rimraf node_modules",
    "type-check": "tsgo --pretty --noEmit && tsc --pretty --noEmit -p test/tsconfig.json"
  },
  "dependencies": {
    "@stdlib/stats": "^0.1.1",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@swc-node/register": "^1.9.0",
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.4",
    "@types/node": "^22.8.6",
    "@typescript/native-preview": "7.0.0-dev.20260309.1",
    "chokidar": "^3.5.3",
    "prettier": "3.6.2",
    "rimraf": "^3.0.2",
    "typescript": "5.7.3",
    "vitest": "^3.2.4"
  },
  "license": "Mixed"
}
