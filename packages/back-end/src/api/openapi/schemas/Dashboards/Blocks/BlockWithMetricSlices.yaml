type: object
required:
  - pinSource
  - pinnedMetricSlices
properties:
    pinSource:
      type: string
      enum:
        - experiment
        - custom
        - none
    pinnedMetricSlices:
      type: array
      items:
        type: string
