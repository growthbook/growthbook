allOf:
  - $ref: "./FeatureBaseRule.yaml"
  - type: object
    required:
      - type
      - value
    properties:
      type:
        type: string
        enum: ["force"]
      value:
        type: string
