tags:
  - ingestion
summary: Get data to use for enrichment of tracking events
operationId: getDataEnrichment
x-codeSamples:
  - lang: 'cURL'
    source: |
      curl https://api.growthbook.io/api/v1/ingestion/data-enrichment \
        -u secret_abc123DEF456:
responses:
  "200":
    content:
      application/json:
        schema:
          type: object
          required:
            - sdkData
          properties:
            sdkData:
              $ref: "../schemas/SdkData.yaml"
