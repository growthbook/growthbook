parameters:
  - $ref: "../../parameters/id.yml"
get:
  summary: Get a single experiment
  operationId: getExperiment
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            required:
              - experiment
            properties:
              experiment:
                $ref: "../../schemas/Experiment.yml"