parameters:
  - $ref: "../parameters.yaml#/id"
tags:
  - namespaces
summary: Get a single namespace
operationId: getNamespace
x-codeSamples:
  - lang: "cURL"
    source: |
      curl https://api.growthbook.io/api/v1/namespaces/ns_123abc \
        -u secret_abc123DEF456:
responses:
  "200":
    content:
      application/json:
        schema:
          type: object
          required:
            - namespace
          properties:
            namespace:
              $ref: "../schemas/Namespace.yaml"
