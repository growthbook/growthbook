summary: Get list of custom fields
tags:
  - custom-fields
parameters:
  - $ref: "../parameters.yaml#/projectId"
operationId: getCustomFields
x-codeSamples:
  - lang: "cURL"
    source: |
      curl https://api.growthbook.io/api/v1/custom-fields?projectId=prj_5l652 \
        -u secret_abc123DEF456:
responses:
  "200":
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "../schemas/CustomField.yaml"
