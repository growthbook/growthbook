summary: Get all namespaces
tags:
  - namespaces
parameters: []
operationId: listNamespaces
x-codeSamples:
  - lang: "cURL"
    source: |
      curl https://api.growthbook.io/api/v1/namespaces \
        -u secret_abc123DEF456:
responses:
  "200":
    content:
      application/json:
        schema:
          type: object
          required:
            - namespaces
          properties:
            namespaces:
              type: array
              items:
                $ref: "../schemas/Namespace.yaml"
