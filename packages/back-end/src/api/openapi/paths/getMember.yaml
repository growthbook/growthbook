parameters:
  - $ref: "../parameters.yaml#/id"
summary: Get a single member
tags:
  - member
operationId: getMember
x-codeSamples:
  - lang: "cURL"
    source: |
      curl https://api.growthbook.io/api/v1/members/member_id \
        -u secret_abc123DEF456:
responses:
  "200":
    content:
      application/json:
        schema:
          type: object
          required:
            - member
          properties:
            member:
              $ref: "../schemas/Member.yaml"
