parameters:
  - $ref: "../parameters.yaml#/id"
tags:
  - saved-groups
summary: Update a single saved group
operationId: postSavedGroup
x-codeSamples:
  - lang: 'cURL'
    source: |
      curl -X PUT https://api.growthbook.io/api/v1/saved-groups \
        -d '{}' # TODO: Example JSON
        -u secret_abc123DEF456:
requestBody:
  required: true
  content:
    application/json:
      schema:
        type: object
        required: []
        properties: []
responses:
  "200":
    content:
      application/json:
        schema:
          type: object
          required:
            - savedGroup
          properties:
            savedGroup:
              $ref: "../schemas/SavedGroup.yaml"
