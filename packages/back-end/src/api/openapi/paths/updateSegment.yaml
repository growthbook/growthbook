parameters:
  - $ref: "../parameters.yaml#/id"
tags:
  - segments
summary: Update a single segment
operationId: updateSegment
x-codeSamples:
  - lang: "cURL"
    source: |
      curl -X POST https://api.growthbook.io/api/v1/segment/seg_123abc \
        -d '{"name":"User Region"}' \
        -u secret_abc123DEF456:
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: "../payload-schemas/UpdateSegmentPayload.yaml"
responses:
  "200":
    content:
      application/json:
        schema:
          type: object
          required:
            - segment
          properties:
            segment:
              $ref: "../schemas/Segment.yaml"
