parameters:
  - $ref: "../../parameters/id.yml"
get:
  summary: Get a single sdk connection
  operationId: getSdkConnection
  responses:
    "200":
      content:
        application/json:
          schema:
            type: object
            required:
              - sdkConnection
            properties:
              sdkConnection:
                $ref: "../../schemas/SdkConnection.yml"