import ParamsRow from "../../components/ParamsRow";

# Authentication

<div>
  Some requests require authentication. You can authenticate requests using your{" "}
  <code>access_token</code>. You can create an <code>access_token</code> in your
  account settings <strong>Settings -> API Keys</strong>. Click on 'Create
  Access Token' to create a new token. You can also delete existing tokens from
  this page.
</div>

<img
  className="mt-5"
  src="/images/apidocs/api_access_tokens.png"
  alt="Create an API Access Token"
  style={{ width: "100%" }}
/>

<span>
  An{" "}
  <span className="bg-green-400 dark:bg-green-600 p-1 rounded text-xs ml-1">
    auth
  </span>{" "}
  tag and the following will be displayed for all APIs that require
  authentication:
</span>

<section className="text-sm my-5">
  <div className="text-lg font-bold">Headers</div>
  <ParamsRow
    name="Authorization"
    type="Bearer <access_token>"
    description="access_token for API authentication"
    hr={false}
  />
</section>
