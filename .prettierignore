.next
node_modules
# Generated by `next typegen` during CI, before pretty:check runs. Its bytes are
# Next's to choose, so a version bump must not fail an unrelated CI step.
packages/front-end/next-env.d.ts
# Gitignored agent scratch space. Formatting it would fail CI on files that
# never reach the repo, and pnpm pretty would silently rewrite local notes.
.context
public
coverage
*.py
packages/shared/src/sdk-versioning/CAPABILITIES.md
docs/src/partials
docs/.docusaurus
docs/build
dist
*.hbs
packages/back-end/src/templates/javascript.js
packages/shared/types/*.js
docs/static/scripts/reo.js
packages/sdk-js/scripts/bump-version.js

# ESLint writes this with its own stable serializer and emits no trailing
# newline, so every regeneration would otherwise fail pretty:check.
eslint-suppressions.json
